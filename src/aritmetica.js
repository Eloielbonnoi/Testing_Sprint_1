
function sum(a, b) {
  return a + b;
}

function res(c, d) {
  return c - d;
}

function div(e, f) {
  return e / f;
}

function mul(g, h) {
  return g * h;
}

module.exports = {sum, res, div, mul};  //això permet exportar la funció per ser testada

